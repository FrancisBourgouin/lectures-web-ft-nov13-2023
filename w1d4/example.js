const sayGoodMorningToEveryone = function (listOfNames) {
  for (const name of listOfNames) {
    sayGoodMorningButBetter(name);
  }
};

// Hey listen, I'll receive a list at one point, and I'll call it listOfNames so that we know what we're talking about
